Final Cleanup — Three Small Changes

Only four small edits remain. I've applied them below.

Summary of the four fixes

# Fix Location
1 Quiz Q10 wording: "healthy aquatic life" → "commonly encountered general field reference in this learning resource" Tab 7
2 Simulator: added explicit note that thresholds are simplified teaching values, not guideline values Tab 9 disclaimer
3 Button "Healthy" → "Reference scenario"; function renamed setHealthy() → setReference() Tab 9
4 Opening line: "health, safety, and suitability" → "water quality, safety and suitability" Tab 1

---

1. Quiz Q10 — find this:

```html
<div class="quiz-question" data-correct="A"><div class="quiz-question-text">10. What is a typical DO level for healthy aquatic life?</div>
```

Replace with:

```html
<div class="quiz-question" data-correct="A"><div class="quiz-question-text">10. What is a commonly encountered DO saturation range used as a general field reference in this learning resource?</div>
```

---

2. Simulator disclaimer — find this:

```html
<p style="font-size: 0.9rem; margin: 0;">The relationships used here are simplified for learning and do not represent the full complexity of real aquatic systems. Simulator outputs must not be used to assess actual water quality or replace field measurements. Real water-quality interpretation requires appropriate local or site-specific guideline values and consideration of the full context.</p>
```

Replace with:

```html
<p style="font-size: 0.9rem; margin: 0;">The relationships used here are simplified for learning and do not represent the full complexity of real aquatic systems. Simulator outputs must not be used to assess actual water quality or replace field measurements. Real water-quality interpretation requires appropriate local or site-specific guideline values and consideration of the full context.</p>
<p style="font-size: 0.9rem; margin: 0.5rem 0 0;">Any numeric thresholds used within the simulator are <strong>simplified teaching values only</strong> — they are not guideline values and must not be used as field decision thresholds.</p>
```

---

3. Simulator button + function — find this:

```html
<button class="btn btn-outline" onclick="setHealthy()"><i class="fas fa-check-circle"></i> Healthy</button>
```

Replace with:

```html
<button class="btn btn-outline" onclick="setReference()"><i class="fas fa-check-circle"></i> Reference scenario</button>
```

And find this JavaScript function:

```javascript
function setHealthy() {
  document.getElementById('simTemp').value = 15;
  document.getElementById('simNitrate').value = 0.8;
  document.getElementById('simPhosphate').value = 0.04;
  document.getElementById('simLight').value = 60;
  document.getElementById('simTime').value = 12;
  document.getElementById('simSalinitySlider').value = 0.5;
  updateSimulator();
}
```

Replace with:

```javascript
function setReference() {
  document.getElementById('simTemp').value = 15;
  document.getElementById('simNitrate').value = 0.8;
  document.getElementById('simPhosphate').value = 0.04;
  document.getElementById('simLight').value = 60;
  document.getElementById('simTime').value = 12;
  document.getElementById('simSalinitySlider').value = 0.5;
  updateSimulator();
}
```

---

4. Tab 1 opening — find this:

```html
<p style="font-size:1.1rem; margin-bottom:1.5rem;">Water is essential for life, ecosystems, agriculture, and industry. Testing tells us about its health, safety, and suitability for different uses. It's the foundation of good water management.</p>
```

Replace with:

```html
<p style="font-size:1.1rem; margin-bottom:1.5rem;">Water is essential for life, ecosystems, agriculture, and industry. Water testing provides information about water quality, safety and suitability for different uses. It's the foundation of good water management.</p>
```

---

Complete Updated Section (single copy-paste block)

For convenience, here is the complete updated <section> containing all four changes. However, because these four edits are in different parts of the file (Tab 1, Tab 7, Tab 9 markup, and the JS), it's cleaner to apply the four targeted edits shown above rather than pasting a large block.

If you'd prefer the complete updated index.html file instead, let me know and I'll generate it — but the four edits above are minimal, targeted, and won't touch anything else in your file.

---

After these four edits, your resource is finished

Final state:

Area Status
Scientific framing ✅ Contextual, no universal thresholds
AHCLPW313 learning support ✅ Aligned with unit requirements
Sampling & QA/QC ✅ Strong
Safety ✅ Comprehensive
Australian/Tasmanian context ✅ Appropriate
Simulator ✅ Clearly labelled teaching model, no "healthy" claims
Quiz ✅ Fully consistent with contextual wording
Glossary ✅ Consistent with Results tab
Preservation guidance ✅ Method/lab specific

No further major work needed. The resource is now internally consistent, scientifically defensible, and VET-appropriate.

If you want, I can also do a final pass to:

· Add a "Last reviewed" date to the footer
· Add version numbering (e.g. v2.0) for your own tracking
· Add a small "Print this guide" button in the resources tab

But those are optional polish items — the content itself is now finished.
